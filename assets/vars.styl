
$header ?= #45494A;
$text ?= #393D3F;
$blue_l ?= #2D71F7;
$blue_d ?= #244891;

shadow()
  box-shadow: 0px 1px 2px rgba(#000, 0.18), 0px 1px 2px rgba(#000, 0.04), 0px 2px 6px rgba(#000, 0.04);

circle(unit)
  width: unit;
  height: unit;
  border-radius: 50%;

btn()
  display: inline-block;
  border-radius: 5px;
  // font-weight: bold;
  cursor: pointer;
  white-space: nowrap;
  text-decoration: none;